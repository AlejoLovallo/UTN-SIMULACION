#ifndef RUTINAS_DOT_H    /* This is an "include guard" */
#define RUTINAS_DOT_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/
int generarIntervaloDeAtencion();
float generarRandom();
float generarTAdeCorte();
float generarTAdeColor();
void rutinaDeCorte(float T);
void rutinaFinalizacion();
int rutinaEleccionDePeluquero();
int rutinaAtencionSinPreferenciaCorte();

#endif /* RUTINAS_S_H */